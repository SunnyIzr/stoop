<div class="container">
  <div class="row">
    
    <div class="col-sm-4 col-sm-push-8">
      <%= render partial: 'layouts/nav' %>
    </div>
    
    <div class="col-sm-8 col-sm-pull-4">
      <div class="row">
        <div class="col-xs-12 building-neighborhood-bar">
          <a href="#" style="border-right:2px solid white; padding-right:20px;">My Building</a>
          <a href="#" class="active" style="padding-left:20px;">My Neighborhood</a>
          <!--NOTE: I recommend toggling the class "active" on/off with jquery-->
        </div>
        
        <%= render partial: 'posts/new' %>
      </div>
    </div>
  </div>
</div>

<div class="container after-profile">
  <div class="row">
    <div class="col-sm-8">
      <div class='posts row'>
        <% @posts.each do |post| %>
      
        <%= render partial: 'post', locals: {post: post} %>
      
        <% end %>
        
      </div>
      <div id="infinite-scrolling">
        <%= will_paginate @posts %>
      </div>
    </div>
    
    <div class="col-sm-4 side-bar-margin">

    </div>
  </div>
</div>



<!-- HIDDEN SKELETON ELEMENTS: POST, COMMENT, NEW COMMENT FORM -->
<%= render partial: 'post', locals: {post: Post.new} %>

<%= render partial: 'comment', locals: {comment: Comment.new} %>

<%= render partial: 'comment_field', locals: {comment: Comment.new} %>
